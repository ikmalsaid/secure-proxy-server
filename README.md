# secure-proxy-server
Mini full-stack app for JavaScript coding practice

## Features
- A frontend HTML form for login and POST data

- The backend duo (/auth/login, /proxy)

- Uses JWT stored in memory (for testing)

- Vanilla JS (no frontend frameworks)